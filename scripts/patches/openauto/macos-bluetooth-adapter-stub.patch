diff --git a/src/autoapp/UI/SettingsWindow.cpp b/src/autoapp/UI/SettingsWindow.cpp
index d29d7ba..523a4d4 100644
--- a/src/autoapp/UI/SettingsWindow.cpp
+++ b/src/autoapp/UI/SettingsWindow.cpp
@@ -249,6 +249,13 @@ namespace f1x::openauto::autoapp::ui {
   }
 #endif
 #ifdef Q_OS_MAC
+  // SettingsWindow references BluetoothAdapterLister but openauto never
+  // declares the class — provide a stub so the macOS build links.
+  class BluetoothAdapterLister {
+   public:
+    QStringList listAdapters() { return {}; }
+  };
+
   void SettingsWindow::populateBluetoothComboBoxMac(QComboBox *comboBoxBluetooth) {
     BluetoothAdapterLister lister;
     QStringList adapters = lister.listAdapters();
