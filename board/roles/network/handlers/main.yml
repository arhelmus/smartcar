# networkctl reload re-reads .network files without bouncing the daemon,
# so the usb0 link (and our SSH session) stays up.
- name: reload networkd
  ansible.builtin.command: networkctl reload
  changed_when: true
